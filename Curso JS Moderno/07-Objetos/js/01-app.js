// en los objetos se pueden agrupar diferentes propiedades en una sola variable

// creando 3 variables 
const nombre = "Monitor 20 pulgadas",
  precio = 300,
  disponible = true;

// creando un objeto, el cual se abre con {}, se establecen las llaves (keys) y sus respectivos valores separados por :
const producto = {
    nombre: "Monitor de 20 pulgadas",
    precio: 300,
    disponible: true
}

