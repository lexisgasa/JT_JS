const producto = {
    nombre: "Monitor de 20 pulgadas",
    precio: 300,
    disponible: true,
  };

//   el metodo .keys se usa para obtener las llaves del objeto como tal
  console.log(Object.keys(producto));
//   mientras que el metodo .values se usara para obtener los valores del objeto
  console.log(Object.values(producto));
