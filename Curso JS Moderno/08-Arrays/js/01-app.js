// arrays se crean igual que variables u objetos, pero en vez de iniciar con "" o {}, inicializa con []
// ademas de separar valores mediante , 
const numeros = [1, 2, 3, 4, 5];

// tambien se pueden crear de la siguiente manera
const meses = new Array("Enero", "Febrero", "Marzo");

console.log(numeros);
console.log(meses)