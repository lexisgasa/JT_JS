const numeros = [10, 20, 30, 40, 50]

// se pueden imprimir los valores de los arrays igual que de cualquier otro tipo de dato, pero es muy visual usar el .table
console.log(numeros)
console.table(numeros)

// se puede acceder a un valor en concreto a traves de su indice - posicion en el array
console.log(numeros[3])