// estructuras de control en js revisan si se cumple cierta condicion mediante el operador "if", si la cumple realizara un codigo especificado, sino, otro

const puntuacion = 999;

// un = sirve para asignar, == para comparar === para comparar estrictamente
if (puntuacion === 1000) {
    console.log("si es igual")
} else {
    console.log("NO es igual")
}

// el !== compara como NO, lo inverso
if (puntuacion !== 1000) {
    console.log("ES DIFERENTE")
} 