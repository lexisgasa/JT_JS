const dinero = 500;
const totalAPagar = 300;

// el simbolo > significa que es MAYOR que, >= significa MAYOR o iGUAL que, en este caso si la cantidad de dinero es mayor que la cantidad a pagar ejecuta esta parte.

if (dinero >= totalAPagar) {
    console.log("Se puede comprar")
} else {
    console.log("No hay dinero suficiente")
}

// el simbolo < significa MENOR que, de igual manera tambien existe <= MENOR o IGUAL que